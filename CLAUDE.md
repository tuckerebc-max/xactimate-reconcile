# Claude entry point

Read `AGENTS.md` and `.agents/skills/xactimate-reconcile/SKILL.md`. Use the same local Python commands and data contracts. No Codex API is embedded. If automatic skill discovery differs, follow the skill file explicitly and use `python xr.py` from the repository root.

Claude execution has not been validated in this release. Do not infer a professional review or human approval from either model's output.
