# Xactimate Task C return

**Completed research/build handoff and working synthetic prototype.** Start with C_ARCHITECTURE.md. Use C_BUILD_PROMPT.md for the next hour of implementation.

- C_SKILL_HARVEST.md: inspected source copies, hashes, methods and conflict resolution.
- C_SOURCES.json: official URLs, retrieval dates, narrow claims and limitations.
- C_VALIDATION.md: actual checks, scorecard and open dependencies.
- prototype/, schemas/, tests/: executable core, contract and 48 tests.
- fixtures/: complete synthetic case and hashed originals.
- examples/: generated memo, operator checklist, cover letter, evidence appendix and machine-readable report.

From this directory:

```bash
python3 -m unittest discover -s tests -v
python3 -m prototype.render fixtures/synthetic_case.json --source-root fixtures --out-dir examples
```

The fixture compares 4,650.00 with 5,290.00 USD in direct pre-tax costs: +840.00 in increases and −200.00 in reductions, net +640.00. Repair and carrier-correct items remain unchanged. Coverage/payment amounts remain unknown. All outputs are synthetic held drafts. Nothing was installed or sent; no revised Xactimate estimate is claimed.
